# CLAUDE.md — LeaveEasy

> เอกสารนี้ให้ AI (Claude Code) อ่านก่อนแก้โค้ดในโปรเจกต์นี้ทุกครั้ง เนื้อหาสรุปมาจาก `leaveeasy-spec.md` — ถ้าเอกสารสองฉบับขัดกัน ให้ยึด `leaveeasy-spec.md` เป็นหลักแล้วแก้ไฟล์นี้ตาม

## 1. ระบบนี้ทำอะไร

LeaveEasy คือ **ระบบขอลาออนไลน์ของที่ทำงาน** แทนที่การขอลาด้วยใบกระดาษ แชท หรือบอกปากเปล่า ที่ทำให้ใบลาหายและไม่มีใครยืนยันได้ว่าอนุมัติแล้วหรือยัง

**เส้นทางหลัก:** พนักงานยื่นใบลา → หัวหน้าพิจารณา → อนุมัติหรือไม่อนุมัติ → บันทึกผลให้ทุกฝ่ายเปิดดูย้อนหลังได้

**ผู้ใช้ 3 บทบาท** (เก็บใน field `role` เป็นรหัสภาษาอังกฤษเท่านี้เท่านั้น):

| role | บทบาท | ทำอะไรได้หลักๆ |
|---|---|---|
| `employee` | ผู้ขอลา | ยื่นใบลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นในใบของตัวเอง · ลบใบของตัวเองที่ยังเป็น `รอพิจารณา` |
| `manager` | ผู้อนุมัติ | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ |
| `hr` | ฝ่ายบุคคล | ทำได้ทุกอย่างของผู้อนุมัติ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · เห็นแดชบอร์ดสรุป |

ผู้ขอลาเปลี่ยนสถานะใบลาเองไม่ได้ และแก้ประเภทการลาไม่ได้ ไม่ว่าบทบาทไหนก็ลบใบลาของคนอื่นไม่ได้

## 2. Collection ทั้งหมดใน Firestore

โครงสร้างข้อมูลเก็บเป็น 3 collection หลัก + 1 subcollection ที่ซ้อนอยู่ใน `leaveRequests`:

```
📁 users/            📄 { name, email, role }
📁 leaveTypes/        📄 { name }
📁 leaveRequests/     📄 { title, reason, status,
                            requesterId, requesterName,
                            approverId, approverName,
                            leaveTypeId, leaveTypeName,
                            startDate, endDate, createdAt,
                            attachmentUrl }   ← attachmentUrl เพิ่มสัปดาห์ที่ 9 เท่านั้น
   📁 approvals/      📄 { authorId, authorName, message, createdAt }
```

**ชื่อ field ต้องตรงตัวพิมพ์เล็ก-ใหญ่ทั้งระบบเสมอ** — `status` กับ `Status` คือคนละ field ใน Firestore และจะทำให้ระบบพังแบบเงียบๆ

**ข้อมูลที่จดซ้ำ (denormalize) — ต้องอัปเดตคู่กับ id เสมอ อย่าลืมฝั่งใดฝั่งหนึ่ง:**

| field ที่จดซ้ำ | จดซ้ำมาจาก |
|---|---|
| `leaveRequests.requesterName` | `users.name` (ของ `requesterId`) |
| `leaveRequests.approverName` | `users.name` (ของ `approverId`) |
| `leaveRequests.leaveTypeName` | `leaveTypes.name` (ของ `leaveTypeId`) |
| `approvals.authorName` | `users.name` (ของ `authorId`) |

Firestore ไม่มี JOIN — ถ้าแก้ชื่อใน `users` ใบลาเก่าจะยังขึ้นชื่อเดิม ยอมรับข้อจำกัดนี้ ไม่ต้องเขียนระบบไล่อัปเดตชื่อซ้ำ

## 3. สถานะใบลา (status) — ใช้ได้ 3 ค่านี้เท่านั้น

```
รอพิจารณา  →  อนุมัติ
           └→  ไม่อนุมัติ
```

- ใบลาที่สร้างใหม่ทุกใบเริ่มต้นที่ `รอพิจารณา` เสมอ ระบบตั้งให้อัตโนมัติ ไม่ให้ผู้ใช้เลือกเอง
- เปลี่ยนได้ตามลูกศรเท่านั้น จาก `รอพิจารณา` ไปเป็น `อนุมัติ` หรือ `ไม่อนุมัติ` — **ห้ามย้อนกลับ** และเมื่อถึง `อนุมัติ`/`ไม่อนุมัติ` แล้วคือปลายทาง เปลี่ยนต่อไม่ได้
- เปลี่ยนสถานะได้เฉพาะ `manager` และ `hr` เท่านั้น (`employee` เปลี่ยนไม่ได้)
- ทุกครั้งที่เปลี่ยนสถานะ **แก้เฉพาะ field `status` เท่านั้น ห้ามเขียนทับ field อื่นในไฟล์เดิม**
- เปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นใน subcollection `approvals` อย่างน้อย 1 รายการมาก่อนเสมอ
- ลบใบลาได้เฉพาะใบที่สถานะยังเป็น `รอพิจารณา` เท่านั้น

## 4. ห้ามมี API key หรือรหัสลับปรากฏในโค้ดหรือไฟล์นี้

- **ห้าม hardcode** ค่า Firebase config, OpenRouter API key หรือรหัสลับใดๆ ลงในไฟล์ `.html`, `.js`, `CLAUDE.md`, หรือไฟล์อื่นใดที่จะขึ้น GitHub
- ค่าลับต้องอยู่ในไฟล์ที่ถูก ignore ไว้ใน `.gitignore` เท่านั้น (เช่นไฟล์ config แยกที่ไม่ commit) — ตรวจสอบว่า `.gitignore` ครอบคลุมไฟล์เหล่านั้นก่อนสร้าง
- ห้ามใส่ตัวอย่างค่าลับจริงแม้ในเอกสารประกอบหรือ commit message เพื่อ "ยกตัวอย่าง"
- ถ้าเผลอ commit รหัสลับไปแล้ว ต้องแจ้งผู้ใช้ทันทีเพื่อหมุนกุญแจ (rotate key) ไม่ใช่แค่ลบไฟล์ทิ้ง
- กติกานี้มีผลตั้งแต่วันนี้ และสำคัญเป็นพิเศษตั้งแต่สัปดาห์ที่ 7 ที่โค้ดจะขึ้น GitHub จริง

## ขอบเขต — สิ่งที่ยังไม่ทำใน Module นี้

ห้ามเพิ่มฟีเจอร์ที่ไม่ได้ระบุในสเปก และห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า (ดู `leaveeasy-spec.md` หัวข้อ 8–9) เช่น ไม่ใช้ framework, ไม่เขียนเซิร์ฟเวอร์เอง, ไม่มี pagination, ไม่มีการแจ้งเตือน, ไม่มีการคำนวณวันลาคงเหลือ ถ้าคิดว่าจำเป็นให้เสนอเป็นรายการแล้วรอคำสั่งก่อน
