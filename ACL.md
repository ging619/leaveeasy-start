# ACL.md — สิทธิ์การใช้งานตามบทบาท (LeaveEasy)

> อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (บทบาท) และหัวข้อ 6 (สถานะและการเปลี่ยนสถานะ) — ตารางนี้คือแหล่งอ้างอิงเดียวสำหรับสิทธิ์แต่ละบทบาท ใช้เป็นฐานในการจำกัดปุ่ม/เมนูบนหน้าจอตอนนี้ และใช้เขียน Firestore Security Rules รายห้องในสัปดาห์ที่ 8

## ตารางสิทธิ์ 3 บทบาท

| บทบาท (`role`) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่ยังเป็น `รอพิจารณา` | **เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ)** · **อนุมัติใบลาของตัวเอง** · **ดูใบลาของคนอื่น** · แก้ประเภทการลา · กำหนดผู้อนุมัติ |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ประเภทการลา · กำหนดผู้อนุมัติ |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · เห็นแดชบอร์ดสรุป | ลบใบลาของคนอื่น |

## จุดที่ต้องเน้นเป็นพิเศษ

- **พนักงานอนุมัติใบลาของตัวเองไม่ได้** — ปุ่มอนุมัติ/ไม่อนุมัติซ่อนจากพนักงานเสมอ ไม่ว่าจะเป็นใบของตัวเองหรือของคนอื่น เพราะพนักงานไม่มีสิทธิ์เปลี่ยนสถานะเลยไม่ว่ากรณีใด
- **พนักงานดูใบลาของคนอื่นไม่ได้** — เป็นข้อจำกัดที่ต้องบังคับทั้งฝั่ง UI (กรองรายการ/กันเปิดหน้ารายละเอียด) และฝั่ง Firestore Security Rules ร่วมกัน

## สถานะการบังคับใช้จริงในสัปดาห์นี้ (สัปดาห์ที่ 7)

ตาม `leaveeasy-spec.md` หัวข้อ 2 ("ข้อจำกัดรายบทบาทในตารางนี้เริ่มมีผลจริงในสัปดาห์ที่ 8") และหัวข้อ 8 (แผนการเติบโตรายสัปดาห์):

- ✅ **บังคับใช้แล้วสัปดาห์นี้:** ปุ่ม **อนุมัติ/ไม่อนุมัติ** ในหน้ารายละเอียดใบลา ซ่อนสำหรับ `role = employee` (แสดงเฉพาะ `manager`/`hr`) — อ่าน role จริงจากโฟลเดอร์ `users/<uid>` ของผู้ล็อกอิน
- ⏳ **ยังไม่บังคับสัปดาห์นี้ (รอสัปดาห์ที่ 8):** การกันไม่ให้พนักงานเห็น/เปิดใบลาของคนอื่น (ทั้งหน้ารายการและหน้ารายละเอียด) · การซ่อนหน้าจัดการประเภทการลาจากผู้ที่ไม่ใช่ฝ่ายบุคคล · Firestore Security Rules รายห้อง · การกำหนดผู้อนุมัติ/สิทธิ์ฝ่ายบุคคลอื่น ๆ ในตาราง

ทดสอบ: เข้า Firebase Console → Firestore → โฟลเดอร์ `users` → แก้ field `role` ของบัญชีทดสอบจาก `employee` เป็น `manager` → รีเฟรชหน้ารายละเอียดใบลาที่สถานะ `รอพิจารณา` → ปุ่มอนุมัติ/ไม่อนุมัติต้องโผล่ขึ้นมา
